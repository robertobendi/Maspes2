{# page.twig dispatches by entry slug to a bespoke template. Twig tries each
   in order and uses the first that exists, so we look for a page-<slug>.twig
   first and fall back to the generic article renderer. #}
{% extends [
    '@theme/page-' ~ entry.slug ~ '.twig',
    '@theme/_page-default.twig'
] %}
